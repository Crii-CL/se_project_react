import ModalWithForm from "./ModalWithForm";

export default function RegisterModal({
  onClose,
  isModalOpen,
  handleOverlayClick,
}) {
  return (
    <ModalWithForm>
      <div></div>
    </ModalWithForm>
  );
}
